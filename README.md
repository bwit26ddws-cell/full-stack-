# Diwas Adhikari — Full Stack ePortfolio

CET138 Full Stack Development, Assignment 1 ePortfolio.

## Folder structure
```
portfolio/
├── index.html      # Main page (structure)
├── css/
│   └── styles.css  # All styling
├── js/
│   └── script.js   # All interactivity
└── README.md
```

## How to publish on GitHub Pages

1. Create a new repository on GitHub (e.g. `portfolio`).
2. Upload these files keeping the same folder structure (drag the whole `portfolio` folder contents into the repo, or use git):
   ```
   git init
   git add .
   git commit -m "Initial portfolio commit"
   git branch -M main
   git remote add origin https://github.com/<your-username>/<repo-name>.git
   git push -u origin main
   ```
3. In the repository, go to **Settings → Pages**.
4. Under **Source**, select the `main` branch and `/ (root)` folder, then **Save**.
5. After a minute, your site will be live at:
   `https://<your-username>.github.io/<repo-name>/`

## Contact
- Email: bwit26.dsa@ismt.edu.np
- GitHub: github.com/bwit26dsa
